# Where the backend runs. "http://localhost:8002" and "http://localhost:8002/api"
# both work (the app adds /api itself). Leave EMPTY for a production build that
# the backend serves from the same origin.
VITE_API_BASE_URL=http://localhost:8002
